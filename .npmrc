# Force use of pnpm package manager
# This ensures consistency across all environments (local, CI, contributors)

# Prevent npm from being used
engine-strict=true

# pnpm specific settings
auto-install-peers=true
shamefully-hoist=false
strict-peer-dependencies=false

# Security settings
legacy-peer-deps=false
fetch-timeout=60000
